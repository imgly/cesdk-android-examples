package ly.img.editor.catalog

object Secrets {
    // Add "license" key value pair in local.properties file
    const val license = CatalogBuildConfig.LICENSE

    // Add "unsplash_host" key value pair in local.properties file
    const val unsplashHost = CatalogBuildConfig.UNSPLASH_HOST

    // Add "remote_asset_source_host" key value pair in local.properties file
    const val remoteAssetSourceHost = CatalogBuildConfig.REMOTE_ASSET_SOURCE_HOST
}
